name: new_proj
description: "A new Flutter project."
publish_to: 'none' 

version: 1.0.0+1

environment:
  sdk: ^3.10.4

dependencies:
  flutter:
    sdk: flutter

  # --- External Packages (Aligned correctly) ---
  audioplayers: ^6.0.0
  
  # Core Firebase
  firebase_core: ^4.3.0
  firebase_auth: ^6.1.3
  cloud_firestore: ^6.1.1
  provider: ^6.1.5+1
  
  # Kiosk Connection
  mqtt_client: ^10.11.3
  
  # UI & Utilities
  google_fonts: ^6.3.3
  intl: ^0.20.2
  permission_handler: ^11.3.1
  
  # PDF Generation
  pdf: ^3.11.1
  path_provider: ^2.1.4
  open_filex: ^4.5.0
  printing: ^5.13.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

# --- Flutter Configuration ---
flutter:
  uses-material-design: true

  # Assets must be defined here
  assets:
    - assets/alarm_sound.mp3